interface:
  display_name: "Low Latency Systems"
  short_description: "tail-latency compression and deterministic path behavior"
  default_prompt: "build low latency systems workflows for tail-latency compression and deterministic path behavior with concrete diagnostics, limits, and rollout controls."
